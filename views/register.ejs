<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SecureTasker — Register</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <nav class="navbar">
    <a href="/" class="logo">🔒 SecureTasker</a>
    <div class="nav-links">
      <a href="/login" class="nav-link">Login</a>
      <a href="/register" class="nav-link">Register</a>
    </div>
  </nav>

  <div class="auth-wrapper">
    <div class="card auth-card">
      <h1>Create Account</h1>
      <p class="subtitle">Pick a username and password</p>

      <% if (error) { %>
        <div class="alert alert-error"><%= error %></div>
      <% } %>

      <!-- Note: this form has NO server-side validation (Vulnerability #14, CWE-20).
           The patch in routes/auth.js adds Joi schema validation. -->
      <form method="POST" action="/register">
        <div class="form-group">
          <label for="username">Username</label>
          <input type="text" id="username" name="username" required autofocus>
        </div>
        <div class="form-group">
          <label for="password">Password</label>
          <input type="password" id="password" name="password" required>
        </div>
        <button type="submit" class="btn btn-primary w-full">Register</button>
      </form>

      <p class="text-muted mt-2" style="text-align:center;">
        Already have an account? <a href="/login">Sign in</a>
      </p>
    </div>
  </div>
</body>
</html>
